# comment to explain your code
mtcars
?mtcars
?mean
class(mtcars)
summary(mtcars)
?summary
str(mtcars)
?str
table(mtcars$gear)
table(mtcars$carb)
table(mtcars$cyl, mtcars$gear)
table(mtcars$cyl, mtcars$gear, dnn=c("cyl","gear"))
